import { Command } from 'clipanion';
export declare class ContributorsUpdateCommand extends Command {
    static paths: string[][];
    static usage: import("clipanion").Usage;
    execute(): Promise<void>;
}
